<#+
#include "dogen/stitch/types/formatter_stitch.hpp"

namespace dogen {
namespace stitch {

void formatter_stitch::format(const text_template& tt) {
    for (const auto& b : tt.content()) {
        if (b.which() == 0 {
            const auto& mcb(boost::get<mixed_content_block>(b));
            for (const auto& l : mcb.content() {
                if (l.which() == 0) {
                    const auto& s(boost::get<std::string>(l));
#>



<#+
} } }
#>