// -*- C++ -*-
//
// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "dogen.relational/odb/tracing/transform-odb-pgsql.hxx"

#include <cassert>
#include <cstring>  // std::memcpy

#include <odb/schema-catalog-impl.hxx>

#include <odb/pgsql/traits.hxx>
#include <odb/pgsql/database.hxx>
#include <odb/pgsql/transaction.hxx>
#include <odb/pgsql/connection.hxx>
#include <odb/pgsql/statement.hxx>
#include <odb/pgsql/statement-cache.hxx>
#include <odb/pgsql/no-id-object-statements.hxx>
#include <odb/pgsql/container-statements.hxx>
#include <odb/pgsql/exceptions.hxx>
#include <odb/pgsql/no-id-object-result.hxx>

namespace odb
{
  // transform
  //

  const char access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  persist_statement_name[] = "PERSIST_DOGEN_RELATIONAL_TRACING_TRANSFORM";

  const char access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  query_statement_name[] = "QUERY_DOGEN_RELATIONAL_TRACING_TRANSFORM";

  const char access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  erase_query_statement_name[] = "ERASE_QUERY_DOGEN_RELATIONAL_TRACING_TRANSFORM";

  const unsigned int access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  persist_statement_types[] =
  {
    pgsql::timestamp_oid,
    pgsql::timestamp_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::int4_oid,
    pgsql::text_oid,
    pgsql::text_oid
  };

  bool access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // start_
    //
    t[0UL] = 0;

    // end_
    //
    t[1UL] = 0;

    // id_
    //
    if (composite_value_traits< ::dogen::relational::tracing::transform_id, id_pgsql >::grow (
          i.id_value, t + 2UL))
      grew = true;

    // instance_id_
    //
    if (composite_value_traits< ::dogen::relational::tracing::transform_instance_id, id_pgsql >::grow (
          i.instance_id_value, t + 3UL))
      grew = true;

    // run_id_
    //
    if (composite_value_traits< ::dogen::relational::tracing::run_id, id_pgsql >::grow (
          i.run_id_value, t + 4UL))
      grew = true;

    // type_
    //
    t[5UL] = 0;

    // input_
    //
    if (composite_value_traits< ::dogen::relational::tracing::json, id_pgsql >::grow (
          i.input_value, t + 6UL))
      grew = true;

    // output_
    //
    if (composite_value_traits< ::dogen::relational::tracing::json, id_pgsql >::grow (
          i.output_value, t + 7UL))
      grew = true;

    return grew;
  }

  void access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);

    // start_
    //
    b[n].type = pgsql::bind::timestamp;
    b[n].buffer = &i.start_value;
    b[n].is_null = &i.start_null;
    n++;

    // end_
    //
    b[n].type = pgsql::bind::timestamp;
    b[n].buffer = &i.end_value;
    b[n].is_null = &i.end_null;
    n++;

    // id_
    //
    composite_value_traits< ::dogen::relational::tracing::transform_id, id_pgsql >::bind (
      b + n, i.id_value, sk);
    n += 1UL;

    // instance_id_
    //
    composite_value_traits< ::dogen::relational::tracing::transform_instance_id, id_pgsql >::bind (
      b + n, i.instance_id_value, sk);
    n += 1UL;

    // run_id_
    //
    composite_value_traits< ::dogen::relational::tracing::run_id, id_pgsql >::bind (
      b + n, i.run_id_value, sk);
    n += 1UL;

    // type_
    //
    b[n].type = pgsql::bind::integer;
    b[n].buffer = &i.type_value;
    b[n].is_null = &i.type_null;
    n++;

    // input_
    //
    composite_value_traits< ::dogen::relational::tracing::json, id_pgsql >::bind (
      b + n, i.input_value, sk);
    n += 1UL;

    // output_
    //
    composite_value_traits< ::dogen::relational::tracing::json, id_pgsql >::bind (
      b + n, i.output_value, sk);
    n += 1UL;
  }

  bool access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  init (image_type& i,
        const object_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // start_
    //
    {
      ::boost::posix_time::ptime const& v =
        o.start ();

      bool is_null (true);
      pgsql::value_traits<
          ::boost::posix_time::ptime,
          pgsql::id_timestamp >::set_image (
        i.start_value, is_null, v);
      i.start_null = is_null;
    }

    // end_
    //
    {
      ::boost::posix_time::ptime const& v =
        o.end ();

      bool is_null (true);
      pgsql::value_traits<
          ::boost::posix_time::ptime,
          pgsql::id_timestamp >::set_image (
        i.end_value, is_null, v);
      i.end_null = is_null;
    }

    // id_
    //
    {
      ::dogen::relational::tracing::transform_id const& v =
        o.id ();

      if (composite_value_traits< ::dogen::relational::tracing::transform_id, id_pgsql >::init (
            i.id_value,
            v,
            sk))
        grew = true;
    }

    // instance_id_
    //
    {
      ::dogen::relational::tracing::transform_instance_id const& v =
        o.instance_id ();

      if (composite_value_traits< ::dogen::relational::tracing::transform_instance_id, id_pgsql >::init (
            i.instance_id_value,
            v,
            sk))
        grew = true;
    }

    // run_id_
    //
    {
      ::dogen::relational::tracing::run_id const& v =
        o.run_id ();

      if (composite_value_traits< ::dogen::relational::tracing::run_id, id_pgsql >::init (
            i.run_id_value,
            v,
            sk))
        grew = true;
    }

    // type_
    //
    {
      ::dogen::relational::tracing::transform_type const& v =
        o.type ();

      bool is_null (false);
      pgsql::value_traits<
          ::dogen::relational::tracing::transform_type,
          pgsql::id_integer >::set_image (
        i.type_value, is_null, v);
      i.type_null = is_null;
    }

    // input_
    //
    {
      ::dogen::relational::tracing::json const& v =
        o.input ();

      if (composite_value_traits< ::dogen::relational::tracing::json, id_pgsql >::init (
            i.input_value,
            v,
            sk))
        grew = true;
    }

    // output_
    //
    {
      ::dogen::relational::tracing::json const& v =
        o.output ();

      if (composite_value_traits< ::dogen::relational::tracing::json, id_pgsql >::init (
            i.output_value,
            v,
            sk))
        grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // start_
    //
    {
      ::boost::posix_time::ptime& v =
        o.start ();

      pgsql::value_traits<
          ::boost::posix_time::ptime,
          pgsql::id_timestamp >::set_value (
        v,
        i.start_value,
        i.start_null);
    }

    // end_
    //
    {
      ::boost::posix_time::ptime& v =
        o.end ();

      pgsql::value_traits<
          ::boost::posix_time::ptime,
          pgsql::id_timestamp >::set_value (
        v,
        i.end_value,
        i.end_null);
    }

    // id_
    //
    {
      ::dogen::relational::tracing::transform_id& v =
        o.id ();

      composite_value_traits< ::dogen::relational::tracing::transform_id, id_pgsql >::init (
        v,
        i.id_value,
        db);
    }

    // instance_id_
    //
    {
      ::dogen::relational::tracing::transform_instance_id& v =
        o.instance_id ();

      composite_value_traits< ::dogen::relational::tracing::transform_instance_id, id_pgsql >::init (
        v,
        i.instance_id_value,
        db);
    }

    // run_id_
    //
    {
      ::dogen::relational::tracing::run_id& v =
        o.run_id ();

      composite_value_traits< ::dogen::relational::tracing::run_id, id_pgsql >::init (
        v,
        i.run_id_value,
        db);
    }

    // type_
    //
    {
      ::dogen::relational::tracing::transform_type v;

      pgsql::value_traits<
          ::dogen::relational::tracing::transform_type,
          pgsql::id_integer >::set_value (
        v,
        i.type_value,
        i.type_null);

      o.type (v);
    }

    // input_
    //
    {
      ::dogen::relational::tracing::json& v =
        o.input ();

      composite_value_traits< ::dogen::relational::tracing::json, id_pgsql >::init (
        v,
        i.input_value,
        db);
    }

    // output_
    //
    {
      ::dogen::relational::tracing::json& v =
        o.output ();

      composite_value_traits< ::dogen::relational::tracing::json, id_pgsql >::init (
        v,
        i.output_value,
        db);
    }
  }

  const char access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::persist_statement[] =
  "INSERT INTO \"DOGEN\".\"TRANSFORM\" "
  "(\"START\", "
  "\"END\", "
  "\"ID\", "
  "\"INSTANCE_ID\", "
  "\"RUN_ID\", "
  "\"TYPE\", "
  "\"INPUT\", "
  "\"OUTPUT\") "
  "VALUES "
  "($1, $2, $3, $4, $5, $6, to_jsonb($7::jsonb), to_jsonb($8::jsonb))";

  const char access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::query_statement[] =
  "SELECT "
  "\"DOGEN\".\"TRANSFORM\".\"START\", "
  "\"DOGEN\".\"TRANSFORM\".\"END\", "
  "\"DOGEN\".\"TRANSFORM\".\"ID\", "
  "\"DOGEN\".\"TRANSFORM\".\"INSTANCE_ID\", "
  "\"DOGEN\".\"TRANSFORM\".\"RUN_ID\", "
  "\"DOGEN\".\"TRANSFORM\".\"TYPE\", "
  "from_jsonb(\"DOGEN\".\"TRANSFORM\".\"INPUT\"), "
  "from_jsonb(\"DOGEN\".\"TRANSFORM\".\"OUTPUT\") "
  "FROM \"DOGEN\".\"TRANSFORM\"";

  const char access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::erase_query_statement[] =
  "DELETE FROM \"DOGEN\".\"TRANSFORM\"";

  const char access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::table_name[] =
  "\"DOGEN\".\"TRANSFORM\"";

  void access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  result< access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::object_type >
  access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  query (database&, const query_base_type& q)
  {
    using namespace pgsql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement_name,
        text,
        false,
        true,
        q.parameter_types (),
        q.parameter_count (),
        q.parameters_binding (),
        imb));

    st->execute ();
    st->deallocate ();

    shared_ptr< odb::no_id_object_result_impl<object_type> > r (
      new (shared) pgsql::no_id_object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::dogen::relational::tracing::transform, id_pgsql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      erase_query_statement_name,
      text,
      q.parameter_types (),
      q.parameter_count (),
      q.parameters_binding ());

    return st.execute ();
  }
}

namespace odb
{
  static bool
  create_schema (database& db, unsigned short pass, bool drop)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (pass);
    ODB_POTENTIALLY_UNUSED (drop);

    if (drop)
    {
      switch (pass)
      {
        case 1:
        {
          return true;
        }
        case 2:
        {
          db.execute ("DROP TABLE IF EXISTS \"DOGEN\".\"TRANSFORM\" CASCADE");
          return false;
        }
      }
    }
    else
    {
      switch (pass)
      {
        case 1:
        {
          db.execute ("CREATE TABLE \"DOGEN\".\"TRANSFORM\" (\n"
                      "  \"START\" TIMESTAMP NULL,\n"
                      "  \"END\" TIMESTAMP NULL,\n"
                      "  \"ID\" TEXT NOT NULL,\n"
                      "  \"INSTANCE_ID\" TEXT NOT NULL,\n"
                      "  \"RUN_ID\" TEXT NOT NULL,\n"
                      "  \"TYPE\" INTEGER NOT NULL,\n"
                      "  \"INPUT\" JSONB NOT NULL,\n"
                      "  \"OUTPUT\" JSONB NOT NULL)");
          return false;
        }
      }
    }

    return false;
  }

  static const schema_catalog_create_entry
  create_schema_entry_ (
    id_pgsql,
    "",
    &create_schema);
}

#include <odb/post.hxx>
