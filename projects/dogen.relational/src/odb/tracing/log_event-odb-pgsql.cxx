// -*- C++ -*-
//
// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "dogen.relational/odb/tracing/log_event-odb-pgsql.hxx"

#include <cassert>
#include <cstring>  // std::memcpy

#include <odb/schema-catalog-impl.hxx>

#include <odb/pgsql/traits.hxx>
#include <odb/pgsql/database.hxx>
#include <odb/pgsql/transaction.hxx>
#include <odb/pgsql/connection.hxx>
#include <odb/pgsql/statement.hxx>
#include <odb/pgsql/statement-cache.hxx>
#include <odb/pgsql/no-id-object-statements.hxx>
#include <odb/pgsql/container-statements.hxx>
#include <odb/pgsql/exceptions.hxx>
#include <odb/pgsql/no-id-object-result.hxx>

namespace odb
{
  // log_event
  //

  const char access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  persist_statement_name[] = "PERSIST_DOGEN_RELATIONAL_TRACING_LOG_EVENT";

  const char access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  query_statement_name[] = "QUERY_DOGEN_RELATIONAL_TRACING_LOG_EVENT";

  const char access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  erase_query_statement_name[] = "ERASE_QUERY_DOGEN_RELATIONAL_TRACING_LOG_EVENT";

  const unsigned int access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  persist_statement_types[] =
  {
    pgsql::timestamp_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::text_oid
  };

  bool access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // timestamp_
    //
    t[0UL] = 0;

    // run_id_
    //
    if (composite_value_traits< ::dogen::relational::tracing::run_id, id_pgsql >::grow (
          i.run_id_value, t + 1UL))
      grew = true;

    // component_
    //
    if (t[2UL])
    {
      i.component_value.capacity (i.component_size);
      grew = true;
    }

    // severity_
    //
    if (t[3UL])
    {
      i.severity_value.capacity (i.severity_size);
      grew = true;
    }

    // message_
    //
    if (t[4UL])
    {
      i.message_value.capacity (i.message_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);

    // timestamp_
    //
    b[n].type = pgsql::bind::timestamp;
    b[n].buffer = &i.timestamp_value;
    b[n].is_null = &i.timestamp_null;
    n++;

    // run_id_
    //
    composite_value_traits< ::dogen::relational::tracing::run_id, id_pgsql >::bind (
      b + n, i.run_id_value, sk);
    n += 1UL;

    // component_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.component_value.data ();
    b[n].capacity = i.component_value.capacity ();
    b[n].size = &i.component_size;
    b[n].is_null = &i.component_null;
    n++;

    // severity_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.severity_value.data ();
    b[n].capacity = i.severity_value.capacity ();
    b[n].size = &i.severity_size;
    b[n].is_null = &i.severity_null;
    n++;

    // message_
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.message_value.data ();
    b[n].capacity = i.message_value.capacity ();
    b[n].size = &i.message_size;
    b[n].is_null = &i.message_null;
    n++;
  }

  bool access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  init (image_type& i,
        const object_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // timestamp_
    //
    {
      ::boost::posix_time::ptime const& v =
        o.timestamp ();

      bool is_null (true);
      pgsql::value_traits<
          ::boost::posix_time::ptime,
          pgsql::id_timestamp >::set_image (
        i.timestamp_value, is_null, v);
      i.timestamp_null = is_null;
    }

    // run_id_
    //
    {
      ::dogen::relational::tracing::run_id const& v =
        o.run_id ();

      if (composite_value_traits< ::dogen::relational::tracing::run_id, id_pgsql >::init (
            i.run_id_value,
            v,
            sk))
        grew = true;
    }

    // component_
    //
    {
      ::std::string const& v =
        o.component ();

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.component_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.component_value,
        size,
        is_null,
        v);
      i.component_null = is_null;
      i.component_size = size;
      grew = grew || (cap != i.component_value.capacity ());
    }

    // severity_
    //
    {
      ::std::string const& v =
        o.severity ();

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.severity_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.severity_value,
        size,
        is_null,
        v);
      i.severity_null = is_null;
      i.severity_size = size;
      grew = grew || (cap != i.severity_value.capacity ());
    }

    // message_
    //
    {
      ::std::string const& v =
        o.message ();

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.message_value.capacity ());
      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_image (
        i.message_value,
        size,
        is_null,
        v);
      i.message_null = is_null;
      i.message_size = size;
      grew = grew || (cap != i.message_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // timestamp_
    //
    {
      ::boost::posix_time::ptime& v =
        o.timestamp ();

      pgsql::value_traits<
          ::boost::posix_time::ptime,
          pgsql::id_timestamp >::set_value (
        v,
        i.timestamp_value,
        i.timestamp_null);
    }

    // run_id_
    //
    {
      ::dogen::relational::tracing::run_id& v =
        o.run_id ();

      composite_value_traits< ::dogen::relational::tracing::run_id, id_pgsql >::init (
        v,
        i.run_id_value,
        db);
    }

    // component_
    //
    {
      ::std::string& v =
        o.component ();

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.component_value,
        i.component_size,
        i.component_null);
    }

    // severity_
    //
    {
      ::std::string& v =
        o.severity ();

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.severity_value,
        i.severity_size,
        i.severity_null);
    }

    // message_
    //
    {
      ::std::string& v =
        o.message ();

      pgsql::value_traits<
          ::std::string,
          pgsql::id_string >::set_value (
        v,
        i.message_value,
        i.message_size,
        i.message_null);
    }
  }

  const char access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::persist_statement[] =
  "INSERT INTO \"DOGEN\".\"LOG_EVENT\" "
  "(\"TIMESTAMP\", "
  "\"RUN_ID\", "
  "\"COMPONENT\", "
  "\"SEVERITY\", "
  "\"MESSAGE\") "
  "VALUES "
  "($1, $2, $3, $4, $5)";

  const char access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::query_statement[] =
  "SELECT "
  "\"DOGEN\".\"LOG_EVENT\".\"TIMESTAMP\", "
  "\"DOGEN\".\"LOG_EVENT\".\"RUN_ID\", "
  "\"DOGEN\".\"LOG_EVENT\".\"COMPONENT\", "
  "\"DOGEN\".\"LOG_EVENT\".\"SEVERITY\", "
  "\"DOGEN\".\"LOG_EVENT\".\"MESSAGE\" "
  "FROM \"DOGEN\".\"LOG_EVENT\"";

  const char access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::erase_query_statement[] =
  "DELETE FROM \"DOGEN\".\"LOG_EVENT\"";

  const char access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::table_name[] =
  "\"DOGEN\".\"LOG_EVENT\"";

  void access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  result< access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::object_type >
  access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  query (database&, const query_base_type& q)
  {
    using namespace pgsql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement_name,
        text,
        false,
        true,
        q.parameter_types (),
        q.parameter_count (),
        q.parameters_binding (),
        imb));

    st->execute ();
    st->deallocate ();

    shared_ptr< odb::no_id_object_result_impl<object_type> > r (
      new (shared) pgsql::no_id_object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::dogen::relational::tracing::log_event, id_pgsql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      erase_query_statement_name,
      text,
      q.parameter_types (),
      q.parameter_count (),
      q.parameters_binding ());

    return st.execute ();
  }
}

namespace odb
{
  static bool
  create_schema (database& db, unsigned short pass, bool drop)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (pass);
    ODB_POTENTIALLY_UNUSED (drop);

    if (drop)
    {
      switch (pass)
      {
        case 1:
        {
          return true;
        }
        case 2:
        {
          db.execute ("DROP TABLE IF EXISTS \"DOGEN\".\"LOG_EVENT\" CASCADE");
          return false;
        }
      }
    }
    else
    {
      switch (pass)
      {
        case 1:
        {
          db.execute ("CREATE TABLE \"DOGEN\".\"LOG_EVENT\" (\n"
                      "  \"TIMESTAMP\" TIMESTAMP NULL,\n"
                      "  \"RUN_ID\" TEXT NOT NULL,\n"
                      "  \"COMPONENT\" TEXT NOT NULL,\n"
                      "  \"SEVERITY\" TEXT NOT NULL,\n"
                      "  \"MESSAGE\" TEXT NOT NULL)");
          return false;
        }
      }
    }

    return false;
  }

  static const schema_catalog_create_entry
  create_schema_entry_ (
    id_pgsql,
    "",
    &create_schema);
}

#include <odb/post.hxx>
